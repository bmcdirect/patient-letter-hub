// Temporarily disabled - contentlayer/generated import removed
export function BlogCard({
  data,
  priority,
  horizontale = false,
}: {
  data: any;
  priority?: boolean;
  horizontale?: boolean;
}) {
  return (
    <div className="text-center py-4">
      <p className="text-muted-foreground">Blog card temporarily disabled</p>
    </div>
  );
}